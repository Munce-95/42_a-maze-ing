[build-system]
requires = ["setuptools>61"]
build-backend = "setuptools.build_meta"

[tool.setuptools]
py_modules = ["mazegen"]

[project]
name = "mazegen"
version = "1.42"
requires-python = ">= 3.10"